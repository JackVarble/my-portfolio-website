import React from "react";
import Template from "./Template";
import "./projects.scss";

const projects = () => {
  return (
    <section id="projects">
      <Template />
    </section>
  );
};

export default projects;
